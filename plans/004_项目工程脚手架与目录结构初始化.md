## 时间: 2026-03-20 19:08:00 CST（2026-03-20 同步代码：目录以实际 `apps/aiBot/src` 为准，已去除空的 `server/upstream/types` 等占位）

## 任务: 项目工程脚手架与目录结构初始化

# 最终任务包

## 目标

把 `/Users/44alex/work/meland/odyssey-v2/lumiGameBot` 从只有规划文档的状态，落成一个 `apps/aiBot + apps/aiBrainService` 的工程底座：

- 根目录明确使用 `yarn dev:aiBot`
- `apps/aiBot` 页面可打开
- `apps/aiBot` API 可访问
- `PostgreSQL` 可连接

本里程碑只解决工程底座和开发体验，不实现正式业务能力。

## 范围内

- 落定当前仓库工程形态：
  - 根目录 workspace
  - `apps/aiBot`：`Next.js + TypeScript + pages/api`
  - `apps/aiBrainService`：独立应用目录占位
  - `PostgreSQL + pg + drizzle-orm + 手动 SQL`
- 建立统一根级工程配置：
  - `package.json`
  - `yarn.lock`
  - `.gitignore`
  - `plans/`
- 建立 `apps/aiBot` 工程配置：
  - `package.json`
  - `next.config.*`
  - `tsconfig.json`
  - `.eslintrc.*`
  - `.env.example`
  - `src/pages`（含 `pages/api`）
  - `src/message`
  - `src/db`
  - `src/dbInterface`
  - `src/lib`（含 `lib/wallet`）
  - `src/components`（如 `WalletModal`）
  - `tests`（Vitest）
- 建立 `apps/aiBrainService` 基础目录：
  - `package.json`
  - `src/`
- 建立统一运行方式：
  - `yarn install`
  - `yarn dev:aiBot`
  - `yarn dev:aiBrainService`
  - `yarn build`
  - `yarn lint`
  - `yarn test`

## 范围外

- 不在本里程碑内实现 `AIBot(机器人)` 创建、编辑、查询等正式业务页面
- 不在本里程碑内实现 `AI Brain Service(AI大脑)` 的正式业务逻辑
- 不在本里程碑内实现与 `Unity Client` 的正式接口联调

## 当前工程结论

- 当前仓库采用 `apps/` 级目录
- `AIBot(机器人)` 当前先在 `apps/aiBot` 中落成可运行应用
- `AI Brain Service(AI大脑)` 当前先作为 `apps/aiBrainService` 独立目录保留空间
- 当前数据库访问方式统一采用：
  - `pg`
  - `drizzle-orm`
  - 手动 SQL 建表

## 目录规划

### 根目录

- `package.json`
- `yarn.lock`
- `.gitignore`
- `plans/`
- `apps/aiBot`
- `apps/aiBrainService`

### `apps/aiBot`（当前）

- `package.json`
- `next.config.js`
- `tsconfig.json`
- `vitest.config.ts`
- `.eslintrc.json`
- `.env.example`
- `next-env.d.ts`
- `src/pages`（`index.tsx`、`health.tsx`、`api/*`）
- `src/message`
- `src/db`（`pg` + `drizzle` + `001_init_aibot_tables.sql`）
- `src/dbInterface`
- `src/lib`（`env`、`api`、`wallet/*` 等）
- `src/components`
- `tests`

### `apps/aiBrainService`

- `package.json`
- `src/index.js`
- 当前阶段只保留独立应用骨架和占位代码

## 详细任务拆解（按顺序执行）

### 1. 建立根目录 workspace 配置

- 新建根级 `package.json`
- 定义根脚本：
  - `dev:aiBot`
  - `dev:aiBrainService`
  - `build`
  - `lint`
  - `test`
- 验收：
  - 根命令语义完整
  - `yarn` 可从根目录驱动子应用

### 2. 建立 `apps/aiBot` 应用骨架

- 建立 `apps/aiBot/package.json`
- 建立 `apps/aiBot` 的 Next、TS、Lint、Vitest 配置
- 建立 `apps/aiBot/src/pages` 与 `pages/api`
- 建立 `apps/aiBot/src/lib`（含钱包与 API 工具）
- 验收：
  - `yarn dev:aiBot` 后本地页面可打开
  - `/api/health` 可访问

### 3. 初始化 `apps/aiBot/src/message`

- 建立消息与 DTO 层
- 验收：
  - 页面和 API 层都能引用同一份消息类型

### 4. 初始化 `apps/aiBot/src/db`

- 建立 `pg` client / pool
- 建立 `drizzle` client
- 建立 `schema.ts`
- 建立 `types.ts`
- 建立 `001_init_aibot_tables.sql`
- 建立 `/api/db/health`
- 验收：
  - `PostgreSQL` 可连接
  - `API` 可返回数据库健康状态

### 5. 初始化 `apps/aiBot/src/dbInterface`

- 建立领域数据访问封装占位
- 第一批对象：
  - `AIBot`
- 验收：
  - 业务层通过 `dbInterface` 使用 `select()/insert()/update()` 风格

### 6. 初始化 `apps/aiBrainService`

- 建立 `apps/aiBrainService/package.json`
- 建立 `apps/aiBrainService/src/index.js`
- 验收：
  - 仓库目录对后续 `AI Brain Service(AI大脑)` 已留出明确空间

## 关键依赖（系统/服务/数据）

- `/Users/44alex/work/meland/odyssey-v2/lumiterrator-v2`
- `/Users/44alex/work/meland/odyssey-v2/services`

## 风险与缓解

- 风险：目录结构继续沿用根级 `src`，后续再拆 `AI Brain Service(AI大脑)` 代价更高
  缓解：当前即改为 `apps/aiBot + apps/aiBrainService`
- 风险：根目录和子应用脚本不一致
  缓解：根目录 `yarn` 作为唯一正式入口

## 验收标准

- 当前仓库采用 `apps/aiBot + apps/aiBrainService` 工程形态
- `apps/aiBot` 可通过根目录 `yarn dev:aiBot/build/lint/test` 驱动
- `apps/aiBot/src/db/001_init_aibot_tables.sql` 可由用户手工执行建表（仅 `aibots`）

## 待确认问题

- 当前围绕 `apps` 目录基线，无新增待确认问题
