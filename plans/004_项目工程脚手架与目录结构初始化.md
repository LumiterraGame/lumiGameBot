## 时间: 2026-03-25 CST（按 `docs/lumiterra-aibot-product-plan.md` 重新对齐）

## 任务: 当前仓库工程脚手架与目录基线

# 最终任务包

## 目标

说明 `lumiGameBot` 当前仓库的工程底座在最新产品形态中的位置。它只负责当前仓库的本地工程组织与开发体验，不再被误认为最终完整产品形态本身。

## 本文件的权威来源

- 最终产品形态、正式技术框架、正式控制台归属与 Unity 集成方向，统一以 [docs/lumiterra-aibot-product-plan.md](/Users/44alex/work/meland/odyssey-v2/lumiGameBot/docs/lumiterra-aibot-product-plan.md) 为准。
- 本文件只描述当前 `lumiGameBot` 仓库的工程脚手架，不重新定义产品形态。

## 当前仓库的工程定位

- 当前仓库保留：
  - `apps/aiBot`
  - `apps/aiBrainService`
  - `plans/`
  - `docs/`
- 这表示的是“当前工作仓库的工程底座”，不是最终完整产品边界。
- 最终产品形态仍然是：
  - `Unity Client + AIBot Panel + Bot Runtime`
  - `Bot API`
  - `Strategy Engine`
  - `Billing and Entitlement`
  - `Backtest and Replay`
  - `lumiterrator dashboard`

## 当前范围

本 plan 只解决：

- workspace 组织方式
- 根目录脚本
- `apps/aiBot` 的当前可运行壳
- `apps/aiBrainService` 的当前占位目录
- 当前仓库里的数据库与测试基础设施

## 当前目录基线

### 根目录

- `package.json`
- `yarn.lock`
- `.gitignore`
- `docs/`
- `plans/`
- `apps/aiBot`
- `apps/aiBrainService`

### `apps/aiBot`

当前仍然是：

- `Next.js + TypeScript + pages/api`
- `wagmi`
- `PostgreSQL + pg + drizzle-orm + 手动 SQL`

它在最新产品定义中的角色是：

- 当前最小管理壳
- 当前最小 BFF / Bot API 落点候选
- 当前最小注册表与健康检查入口

### `apps/aiBrainService`

当前仍然是：

- 独立应用目录
- 占位或逐步扩展中的服务壳

它在最新产品定义中的角色是：

- `Strategy Engine`
- `AI Advisor`
- `Replay`
- `Risk`

的主要承载位候选。

## 当前工程目标

- 根目录可执行：
  - `yarn install`
  - `yarn dev:aiBot`
  - `yarn dev:aiBrainService`
  - `yarn build`
  - `yarn lint`
  - `yarn test`
- `apps/aiBot` 页面可打开
- `apps/aiBot` API 可访问
- 数据库可连接

## 当前不代表的能力

当前工程底座 **不代表** 以下正式能力已经完成：

- Unity 内 `AIBot Panel`
- `Bot Runtime`
- `Bot API` 完整接口
- `Billing and Entitlement`
- `Strategy Engine`
- `Backtest and Replay`
- `lumiterrator-v2` 中的正式 Dashboard / Replay / Pricing / Subscription 页面

## 当前详细任务拆解

### 1. 根目录 workspace 配置

- 根级 `package.json`
- 根脚本：
  - `dev:aiBot`
  - `dev:aiBrainService`
  - `build`
  - `lint`
  - `test`

### 2. `apps/aiBot` 应用骨架

- `package.json`
- Next / TS / Lint / Vitest 配置
- `src/pages`
- `src/lib`
- `src/message`
- `src/db`
- `src/dbInterface`
- `src/components`

### 3. `apps/aiBot` 数据基础

- `pg`
- `drizzle`
- `schema.ts`
- `types.ts`
- `001_init_aibot_tables.sql`
- `/api/db/health`

### 4. `apps/aiBrainService` 占位骨架

- `package.json`
- `src/`
- 后续可继续承接 Strategy / Replay / Risk

## 关键依赖

- `/Users/44alex/work/meland/odyssey-v2/lumiterrator-v2`
- `/Users/44alex/work/meland/odyssey-v2/services`

## 风险与缓解

- 如果把当前仓库脚手架误写成最终产品形态，会继续和权威文档冲突。
  缓解：本文件明确只定义“当前工程底座”，不定义完整产品。
- 如果把 `apps/aiBot` 误写成唯一正式控制台，会和 `lumiterrator-v2` 的正式 Dashboard 规划冲突。
  缓解：明确 `apps/aiBot` 只是当前最小壳。
- 如果把 `apps/aiBrainService` 误写成已经完成的正式 Brain，会误导后续实现顺序。
  缓解：明确它当前仍是承载位候选，而不是完成态。

## 验收标准

- 本文件明确从 `docs/lumiterra-aibot-product-plan.md` 继承产品形态。
- 本文件不再把 `apps/aiBot + apps/aiBrainService` 写成最终完整产品边界。
- 本文件只描述当前仓库工程底座，与最新产品定义和技术框架不冲突。
