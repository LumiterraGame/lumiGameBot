## 时间: 2026-03-20 19:08:00 CST（2026-03-20 同步代码：`apps/aiBot` 目录与里程碑 1 交付物已对齐现状）

## 任务: 开发计划与任务拆解

# 最终任务包

## 目标

基于 `001` 的产品形态和 `002` 的架构结论，明确当前正式开发顺序、目录落点、依赖关系、验收口径和同步标准。该文件只回答“先做什么、后做什么、产出什么、代码放哪里、怎么验收”。

## 范围内

- 明确当前仓库的正式目录基线：
  - `apps/aiBot`
  - `apps/aiBrainService`
- 明确当前正式开发主线：
  - `AIBot(机器人)` 创建与管理
  - `AI Brain Service(AI大脑)` 应用骨架
  - `Unity Agent` 基本接入架构
  - 世界上下文、地图语义与动作能力扩充
  - 赚钱与成长相关游戏逻辑逐步接入
- 明确每个里程碑的目标、交付物、关键依赖和验收标准
- 明确根目录 `yarn` 运行方式与 `apps` 子应用职责边界

## 范围外

- 不在本文件中重新讨论产品形态是否成立
- 不在本文件中重新讨论 `Unity + AI Brain Service(AI大脑)` 架构是否成立
- 不在本文件中细到函数级实现

## 项目目录结构规划

- 根目录采用 `apps` 组织方式：
  - `apps/aiBot`
  - `apps/aiBrainService`
  - `plans/`
  - 根级 workspace 配置
- 当前目标开发体验是：
  - 根目录 `yarn dev:aiBot`
  - `apps/aiBot` 页面可打开
  - `apps/aiBot` API 可访问
  - 数据库可连接
- `Unity Agent` 不放在当前仓库，继续放在：
  - `/Users/44alex/work/meland/odyssey-v2/unity-client-v2`

### 根目录规划

- `package.json`
- `yarn.lock`
- `.gitignore`
- `plans/`
- `apps/aiBot`
- `apps/aiBrainService`

### `apps/aiBot` 规划（当前落地）

- `package.json`
- `next.config.*`
- `tsconfig.json`
- `.eslintrc.*`
- `.env.example`
- `src/pages`（首页控制台 + `pages/api`）
- `src/message`
- `src/db`（`drizzle` + `pg` + 手动 SQL）
- `src/dbInterface`
- `src/lib`（含 `wallet`）
- `src/components`（钱包弹层等）
- `tests/`（Vitest）

### `apps/aiBrainService` 规划

- `package.json`
- `src/`
- 当前阶段先保留独立应用目录和占位代码
- 后续承接：
  - `AI Brain Service(AI大脑)`
  - `AI Gateway`
  - `session lease`
  - `memory`
  - `decision journal`
  - `pnl ledger`

## 当前工程结论

- `AIBot(机器人)` 与 `AI Brain Service(AI大脑)` 在目录上是同级应用：
  - `apps/aiBot`
  - `apps/aiBrainService`
- 当前正式可运行入口先落在 `apps/aiBot`
- `apps/aiBot` 采用：
  - `Next.js + TypeScript`
  - `pages + api`
  - 共享 `PostgreSQL`
- `apps/aiBrainService` 当前先建立独立应用目录和占位结构，不在本阶段实现正式运行链路
- 当前数据库接入统一采用：`pg` + `drizzle-orm` + 手动维护 SQL 建表脚本（**非 Prisma**）

## 当前运行方式

- 当前项目要求可以通过根目录 `yarn` 统一运行
- 根脚本至少包含：
  - `yarn install`
  - `yarn dev:aiBot`
  - `yarn dev:aiBrainService`
  - `yarn build`
  - `yarn test`
  - `yarn lint`
- 本地开发默认形态：
  - 根目录 workspace
  - `apps/aiBot` 为当前可运行应用
  - `PostgreSQL`
  - `apps/aiBot/src/db/*.sql` 手动建表

## 任务拆解（按顺序执行）

### 里程碑 0：项目工程脚手架与目录结构初始化

- 目标：
  - 把当前仓库变成 `apps/aiBot + apps/aiBrainService` 的 workspace 工程，让根目录 `yarn dev:aiBot` 能拉起 `aiBot` 页面、API 和数据库连接能力
- 交付物：
  - 根目录 workspace `package.json`
  - `apps/aiBot`
  - `apps/aiBrainService`
  - `apps/aiBot/next.config.*`
  - `apps/aiBot/src/pages`（含 `api`）
  - `apps/aiBot/src/message`、`src/db`、`src/dbInterface`、`src/lib`、`src/components`
  - `apps/aiBot/tests`
  - `PostgreSQL` 本地连接配置（`.env.example` / `.env.local`）
- 验收标准：
  - 根目录可执行 `yarn dev:aiBot`
  - `apps/aiBot` 页面可正常打开
  - `apps/aiBot` API 可正常访问
  - `PostgreSQL` 能被正常连接
  - 手动 SQL 文件可由用户执行完成建表

### 里程碑 1：`AIBot(机器人)` 创建与管理基础

- 目标：
  - 先把“用户有没有 `AIBot(机器人)`、怎么创建、怎么管理”做成正式入口
- 交付物（已实现，见 `plans/005`）：
  - 首页单页：`Current` / `Create` / `Update` 三 Tab，`wagmi` 连接钱包（无独立服务端登录验签）
  - API：`GET /api/bots/query/[walletAddress]`、`POST /api/bots/create`、`PATCH /api/bots/update`（`create/update` 请求体带 `walletAddress`，**信任前端**）
  - 数据：`aibots` 表 + `dbInterface/aibot.ts`
- 验收标准：
  - 当前钱包无 `AIBot(机器人)` 时可完成创建
  - 当前钱包有 `AIBot(机器人)` 时可读取和编辑
  - 一个钱包只能创建一个 `AIBot(机器人)`

### 里程碑 2：`AI Brain Service(AI大脑)` 应用骨架

- 目标：
  - 把 `apps/aiBrainService` 建成正式同级应用目录，而不是未来再重新拆分
- 交付物：
  - `apps/aiBrainService/package.json`
  - `apps/aiBrainService/src`
  - 最小启动入口或占位说明
  - 与 `aiBot` 的共享接口预留
- 验收标准：
  - 仓库目录结构稳定
  - 后续 `AI Brain Service(AI大脑)` 能直接在 `apps/aiBrainService` 内扩展

### 里程碑 3：`Unity Agent` 基本接入架构

- 目标：
  - 让 `Unity Client` 成为正式的游戏内 Bot 入口
- 交付物：
  - `AI Agent` 入口
  - `BotAgent` 面板基础骨架
  - 点击 `AI Agent` 后自动查询当前钱包 `AIBot(机器人)`
  - 空状态页：
    - 去 `Web` 创建 `AIBot(机器人)`
    - `重新加载`
- 验收标准：
  - `Unity` 能识别当前钱包是否存在 `AIBot(机器人)`
  - `Unity` 能加载 `AIBot(机器人)` 基础摘要

### 里程碑 4：世界上下文与动作骨架

- 目标：
  - 打通 Bot 从“有配置”到“知道周围环境和可行动作”的最小闭环
- 交付物：
  - 世界上下文采集
  - 地图语义基础
  - 怪物 / `NPC` / 交互目标定位骨架
  - 动作执行骨架：
    - 移动
    - 攻击
    - `use skill`
- 验收标准：
  - `Unity Agent` 能输出结构化本地世界上下文
  - `AI Brain Service(AI大脑)` 后续可消费这些上下文

### 里程碑 5：游戏逻辑接入与行为串联

- 目标：
  - 把 Bot 行为逐步串成完整路线，而不是单点动作
- 交付物：
  - 任务推进
  - 天赋升级
  - 合成
  - 强化
  - 地图目标寻址
  - 多步行为编排
- 验收标准：
  - Bot 可完成基础多步行为链路
  - Bot 可根据地图与目标位置执行连续动作

## 关键依赖（系统/服务/数据）

- `/Users/44alex/work/meland/odyssey-v2/unity-client-v2`
- `/Users/44alex/work/meland/odyssey-v2/services`
- `/Users/44alex/work/meland/odyssey-v2/game-services-v2`
- `/Users/44alex/work/meland/odyssey-v2/theweb3-v2`
- `/Users/44alex/work/meland/odyssey-v2/contracts-monad-v2`

## 风险与缓解

- 风险：目录结构过晚调整，导致后续 `AI Brain Service(AI大脑)` 再次搬家
  缓解：当前即按 `apps/aiBot + apps/aiBrainService` 定型
- 风险：根目录和子应用脚本不一致
  缓解：以根目录 `yarn` 为统一入口，子应用脚本只做内部承接

## 验收标准

- `003` 与 `004/005` 对当前目录基线口径一致
- 当前代码目录与 plans 中的 `apps/aiBot` / `apps/aiBrainService` 一致
- 根目录 `yarn dev:aiBot/build/test/lint` 能围绕 `aiBot` 正常工作

## 待确认问题

- 当前围绕正式 `apps` 目录基线，无新增待确认问题
