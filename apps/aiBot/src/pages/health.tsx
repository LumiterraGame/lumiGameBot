export default function HealthPage() {
  return (
    <main style={{ padding: 24, fontFamily: "sans-serif" }}>
      <h1>Health</h1>
      <p>Use `/api/health` and `/api/db/health` for API checks.</p>
    </main>
  );
}
